with open('golf.txt', 'r') as f:
    lines = f.readlines()
    i = 0
    while i < len(lines):
        name = lines[i].strip()
        score = lines[i+1].strip()
        print(f"Name:{name}\nScore:{score}\n")
        i += 2